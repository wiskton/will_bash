# git commit
pull(){
    git pull origin master
}

push(){
    git push origin master
}

cm() {
    git commit -m "$1" -a
}

# git add all & commit
cma() {
    git add .
    git commit -m "$1" -a
}

s(){
    git status
}