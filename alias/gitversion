# git commit
pull(){
    git pull origin "${1:-master}"
}

push(){
    git push origin "${1:-master}"
}

cm() {
    git commit -m "$1" -a
}

# git add all & commit
cma() {
    git add .
    git commit -m "$1" -a
}

s(){
    git status
}
