projeto () {
    dir_name="/projetos/";
    caminho="$HOME$dir_name$1";
    cd "$caminho";

    active="venv/bin/activate"
    venv="$caminho/$active"
    source "$venv"
}

newenv(){
    virtualenv $1 --python=python3
}
